<<객체>>

String()
Array()
Date()
Math.sin()
Number()


var str="welcome";  // 원시타입
var str= new String("welcome");  // 객체타입
	인스턴스

생성자 함수는 항상 대문자로 시작한다.



카멜표기법
낙타등 표기법

var add=(function(){ })(변수1, 변수2);
() ()

콜백함수 : 다음에 실행할 함수
어떤 일이 수행되고, 그 다음에 실행되는 함수.

함수의 매개변수가 함수인 경우
-> 콜백함수

nodeType

태그 -> 1
속성 -> 2
텍스트 -> 3



<h2 title="title">text</h2>

childeren -> 태그들
nodeValue 와 nodeName
childNode로 접근하거나
item(0)으로 접근한다.

nextElementSibling
다음 형제 요소

firstElementChild
첫번째 자식태그

childElementCount
자식요소의 개수

.classList.add() 클래스추가
.classList.remove() 클래스제거
















